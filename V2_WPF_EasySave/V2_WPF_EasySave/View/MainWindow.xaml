<Window x:Class="V2_WPF_EasySave.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:V2_WPF_EasySave.ViewModel"
        xmlns:utils="clr-namespace:V2_WPF_EasySave.Utils"
        Title="Easy Save V2" Height="400" Width="600">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="{utils:Translate SavedJobs}" FontSize="20" Margin="0 0 0 10" />

            <ListView x:Name="JobsListBox"
          ItemsSource="{Binding SavedJobs}"
          SelectionMode="Extended"
          Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="5" Margin="5">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                            <TextBlock Text="État :" />
                            <TextBlock Text="{Binding State}" />

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <Button Content="Pause"
                                        Command="{Binding DataContext.PauseCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Margin="5,0" />
                                <Button Content="Play"
                                        Command="{Binding DataContext.ResumeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Margin="5,0" />
                                <Button Content="Stop"
                                        Command="{Binding DataContext.StopCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Margin="5,0" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            
            <StackPanel Orientation="Horizontal" Margin="0 10 0 0" HorizontalAlignment="Center">
                <Button Content="{utils:Translate Create}" Command="{Binding CreateCommand}" Width="80" />
                <Button Content="{utils:Translate Edit}" Command="{Binding ModifyCommand}" Width="80" />
                <Button Content="{utils:Translate Delete}" Command="{Binding DeleteCommand}" Width="80" />
                <Button Content="{utils:Translate Execute}" Command="{Binding ExecuteCommand}" Width="80" />
                <Button Content="{utils:Translate Refresh}" Command="{Binding RefreshCommand}" Width="80" />
                <Button Content="Softwares" Command="{Binding EditBlockedAppsCommand}" Width="80" />
            </StackPanel>

            <ComboBox SelectedValue="{Binding Source={x:Static utils:LanguageManager.Instance}, Path=CurrentLanguage}"
                      SelectionChanged="OnLanguageChanged"
                      Margin="0,10,0,0"
                      Width="120">
                <ComboBoxItem Content="fr" />
                <ComboBoxItem Content="en" />
            </ComboBox>
        </StackPanel>
    </Grid>
</Window>